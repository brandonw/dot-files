#!/bin/sh

if [[ "$1" == "" || "$2" == "" ]]
then
	echo "Must supply two parameters: original domain name and new domain name."
	exit 1
fi

virt-clone -original=$1 -name=$2 -file=/var/lib/libvirt/images/$2.img
